import { ZoomOnHooverDirective } from './zoom-on-hoover.directive';

describe('ZoomOnHooverDirective', () => {
  it('should create an instance', () => {
    const directive = new ZoomOnHooverDirective();
    expect(directive).toBeTruthy();
  });
});
